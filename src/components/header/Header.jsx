import { TitleHeader } from "./TitleHeader"
import { StyleHeader } from "./styles"

export const Header = () => {
    return(
        <StyleHeader> 
            <TitleHeader />
        </StyleHeader>
    )
}